/speckit-constitution

다음 원칙을 이 프로젝트 전체의 개발 원칙으로 사용해줘.

1. 이 프로젝트는 서울 성북구 보문동 "스카이빌" 원룸(오피스텔)의 임대 홍보를 위한 단일 페이지 정적 웹사이트다.
2. 로그인, 서버, 데이터베이스, API 서버는 만들지 않는다. 매물 정보는 listing.json 정적 파일로 관리한다.
3. 프로젝트 루트의 DESIGN.md(디자인 브리프)를 UI의 최우선 시각 기준으로 사용한다.
4. DESIGN.md 원본 파일은 수정하지 않는다.
5. 모든 페이지 경로와 이미지 경로는 GitHub Pages에서도 동작하도록 상대 경로를 사용한다.
6. semantic HTML과 키보드 접근성을 지킨다.
7. prefers-reduced-motion 사용자는 과도한 애니메이션 없이 콘텐츠를 볼 수 있어야 한다.
8. 사진만 나열하지 않는다. 히어로 리드문과 섹션별(방/옵션/보안/입지/주변환경) 설명 문단을 실제 콘텐츠로 제공한다.
9. 본문은 사진 위의 작은 캡션으로 축소하지 않고, 읽기 좋은 크기와 행간을 가진 독립적인 디자인 요소로 다룬다.
10. 사용되는 모든 사진은 실제 촬영된 매물·현장 사진(제공된 원본 파일)만 사용하고, 온라인에서 대체 이미지를 검색하거나 내려받지 않는다.
11. placeholder 이미지와 임의의 단색 박스, 외부 이미지 hotlink를 최종 결과에 남기지 않는다.
12. 매물 데이터(주소, 가격, 옵션, 연락처 등)는 listing.json 한 곳에서만 관리하고, 다른 호실을 추가할 때는 이 파일과 사진 폴더만 교체해 그대로 재사용할 수 있는 템플릿 구조를 유지한다.
13. 지도는 네이버지도(Naver Maps API)로 표시하고, 정확한 주소(서울특별시 성북구 보문동2가 82)를 그대로 노출한다.
14. 카카오톡 상담 ID(ckpark1001), 전화번호(010-8222-7661), 이메일(ckpark1001@naver.com)은 마스킹 없이 그대로 노출하고, 클릭/탭으로 바로 연결되게 한다(tel:, mailto:, 카톡 ID 복사 버튼).
15. 모바일에서는 읽기 쉬운 세로 스크롤 흐름과, 화면 하단에 고정된(sticky) 카톡/전화 문의 CTA 바를 제공한다.
16. 데스크톱에서는 은은한 스크롤 등장 효과(fade-up 등)를 허용하되, 장면이 끊기거나 강제로 튀는 연출은 쓰지 않는다.
17. 불필요한 프레임워크와 서버 의존성을 추가하지 않는다. 순수 HTML/CSS/JS 또는 경량 정적 사이트 생성기만 사용한다.
18. 최종 결과는 GitHub Pages에서 바로 배포 가능한 구조여야 한다(`/listings/{매물명}/` 폴더 구조 포함).
19. "현재 사진은 같은 구조의 다른 호실 사진입니다" 등 원문의 고지 문구는 표기 그대로 유지한다.
